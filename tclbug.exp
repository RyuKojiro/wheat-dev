set filename "ccounter.bin"
set addr 0
set len [file size $filename]
set fp [open $filename r]

while {$addr < $len} {
	set byte [format %02X [scan [read $fp 1] %c]]
	set offset [format %04x $addr]
	send_user "$offset: $byte\n"
	incr addr
}
